[
  {"id":"rf-fc01","front":"Cartesian product P × Q","back":"Set of ALL ordered pairs (p,q) with p ∈ P, q ∈ Q. If either set is φ, the product is φ."},
  {"id":"rf-fc02","front":"Equality of ordered pairs","back":"(a,b) = (c,d) ⇔ a = c AND b = d. Coordinate-wise."},
  {"id":"rf-fc03","front":"n(A × B)","back":"> n(A) × n(B). Grid picture: rows × columns."},
  {"id":"rf-fc04","front":"Is A × B = B × A?","back":"Generally NO — (a,r) ≠ (r,a). Only equal when the sets force identical pair sets (e.g. both empty, or A = B in special cases)."},
  {"id":"rf-fc05","front":"Number of relations from A to B","back":"Every subset of A × B is a relation ⇒ 2^(n(A)·n(B))."},
  {"id":"rf-fc06","front":"Relation R from A to B","back":"Any subset R ⊆ A × B. In (x,y) ∈ R, y is the image of x."},
  {"id":"rf-fc07","front":"Domain of a relation","back":"Set of all FIRST elements of its ordered pairs — strip seconds and collect."},
  {"id":"rf-fc08","front":"Range vs codomain","back":"Range = actual second elements collected. Codomain = whole set B declared up front. Always range ⊆ codomain."},
  {"id":"rf-fc09","front":"Function definition","back":"A relation where EVERY element of A has ONE AND ONLY ONE image. Domain all used; first coordinates never repeat."},
  {"id":"rf-fc10","front":"Image / preimage","back":"f(a) = b: b is the image of a; a is the preimage of b."},
  {"id":"rf-fc11","front":"Two failure modes for functions","back":"(1) some a ∈ A has no image; (2) some a has two different images. Many-to-one is allowed!"},
  {"id":"rf-fc12","front":"Real valued vs real function","back":"Range ⊆ R ⇒ real valued. Domain also ⊆ R ⇒ real function."},
  {"id":"rf-fc13","front":"Identity function","back":"f(x) = x on R. Graph: 45° line through origin. Domain = range = R."},
  {"id":"rf-fc14","front":"Constant function","back":"f(x) = c. Domain R, range {c}. Horizontal line graph."},
  {"id":"rf-fc15","front":"Polynomial function","back":"f(x) = a₀ + a₁x + … + aₙxⁿ, n non-negative integer, real coefficients. x² + 2/x fails (negative power)."},
  {"id":"rf-fc16","front":"Rational function","back":"f(x)/g(x) with g(x) ≠ 0. Exclude roots of g from domain."},
  {"id":"rf-fc17","front":"Modulus function","back":"|x| = x if x ≥ 0, –x if x < 0. V-graph. Range [0, ∞)."},
  {"id":"rf-fc18","front":"Signum function","back":"sgn(x) = –1 / 0 / 1 according as x < 0 / = 0 / > 0. Range {–1, 0, 1}."},
  {"id":"rf-fc19","front":"Greatest integer [x]","back":"Largest integer ≤ x. [2.7] = 2, [–0.3] = –1. Staircase graph; each step closed-left, open-right."},
  {"id":"rf-fc20","front":"Algebra of functions","back":"(f±g)(x)=f(x)±g(x); (αf)(x)=αf(x); (fg)(x)=f(x)g(x); (f/g)(x)=f(x)/g(x) when g(x)≠0."},
  {"id":"rf-fc21","front":"Linear function","back":"f(x) = mx + c — straight line, slope m, y-intercept c."},
  {"id":"rf-fc22","front":"Vertical line test (beyond syllabus)","back":"A graph is a function iff no vertical line cuts it more than once."},
  {"id":"rf-fc23","front":"Counting functions from A to B (beyond syllabus)","back":"Each of p inputs picks one of q outputs: q^p total functions. NOT 2^(pq) — that counts relations."},
  {"id":"rf-fc24","front":"Domain reflex for fractions","back":"Denominator ≠ 0 first: factor it, exclude every root. Then even roots: inside ≥ 0."},
  {"id":"rf-fc25","front":"Range of x²/(1+x²)","back":"[0, 1): never negative, approaches 1 but never reaches it."}
]
