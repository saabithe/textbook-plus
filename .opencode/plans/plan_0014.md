# Plan: Sets Chapter Rewrite — Fix Crammed Single Lines Only

**Status:** Plan complete, awaiting approval (revised after user feedback)
**Created:** 2026-08-21
**Target file:** `textbook-plus/src/content/maths/sets/page.tsx` (only file touched)

## Scope Rule (per user)

Fix ONLY content that genuinely feels crammed / unnecessarily forced onto one line.
Normal flowing paragraphs (section intros, Historical Note) **stay as paragraphs**.
No aggressive restructuring, no content changes.

## In Scope (confirmed offenders)

### Phase 1 — Worked-example solutions
- **Example 7**: five-set equality comparison (312-char `<p>`) → one bullet per set
  comparison, bold conclusion `C = E` on its own line
- **Example 25**: two-part proof (173-char `<p>`) → numbered steps:
  assume a∈A → a∈A∪B = A∩B → a∈B ⇒ A⊂B; mirror argument ⇒ B⊂A; conclude A=B

### Phase 2 — Note callout in §1.2
- Roster/set-builder note (~500 chars run together): split into two labelled groups,
  "Roster form:" lines and "Set-builder form:" lines, instead of one run-on block

### Phase 3 — Answer-key entries that cram proofs/results onto one line
- **Ex 1.5 key Q4**: De Morgan verification (~330-char single `<li>`) → separate
  lines for part (i) and part (ii)
- **Misc key Q3**: B=C equality chain → each equal step on its own line
- **Misc key Q4**: four-condition equivalence → 4 short implication lines
- **Misc key Q6**: both identities → two sub-lines
- **Misc key Q9**: A=B proof → two short chains + conclusion line
- **Ex 1.2 key Q4(iv)**: wordy explanation → split across 2 short lines

### Phase 4 — Verify (no commit)
- `npx tsc --noEmit -p tsconfig.json`
- `npm run build` from `textbook-plus/`
- Stop. No commit/push until user explicitly says so.

## Explicitly Out of Scope (stays as-is)
- Section intro paragraphs (§1.3, §1.4, §1.6, §1.7, §1.8, §1.10, intervals) — normal prose
- Historical Note — stays prose
- Exercise question text — verbatim NCERT layout unchanged
- Answer keys already in list form (Ex 1.1–1.4, most of 1.5/Misc) — unchanged
